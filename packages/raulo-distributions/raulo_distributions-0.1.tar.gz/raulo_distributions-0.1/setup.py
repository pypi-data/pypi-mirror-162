from setuptools import setup

setup(name='raulo_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['raulo_distributions'],
      author='Raul Serrano Mena',
      zip_safe=False)
