[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hlsdl"
version = "1.0.0"
dependencies = ["aiohttp", "aiohttp_socks", "argparse", "pycryptodome"]
