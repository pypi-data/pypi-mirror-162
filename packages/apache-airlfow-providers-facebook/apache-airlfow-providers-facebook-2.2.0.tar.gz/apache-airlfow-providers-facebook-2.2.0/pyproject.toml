[project]
name = "apache-airlfow-providers-facebook"
version = "2.2.0"
description = "Fix for missing provider due to typo. Do not use directly"
authors = [
  {name="Ash Berlin-Taylor", email="ash@apache.org"}
]
license = {text="Apache 2"}

dependencies = [
  "apache-airflow-providers-facebook>=2.2.0"
]

