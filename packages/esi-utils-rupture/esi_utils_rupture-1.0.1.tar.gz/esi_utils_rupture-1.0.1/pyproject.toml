[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=61.0"]

[project]
authors = [
  {name = "Eric Thompson"},
]
classifiers = [
  "Programming Language :: Python :: 3",
  "Operating System :: OS Independent",
]
description = "USGS Earthquake Impact Utilities for Ruptures"
name = "esi_utils_rupture"
requires-python = ">=3.7"
version = "1.0.1"
