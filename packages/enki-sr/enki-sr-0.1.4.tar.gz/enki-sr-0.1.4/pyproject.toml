[tool.poetry]
name = "enki-sr"
version = "0.1.4"
description = ""
authors = ["Marcus Orochena <marcus.orochena@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.9"
blessed = "^1.19.1"
typer = "^0.6.1"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[tool.poetry.scripts]
enki = "enki_sr.enki:main"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
