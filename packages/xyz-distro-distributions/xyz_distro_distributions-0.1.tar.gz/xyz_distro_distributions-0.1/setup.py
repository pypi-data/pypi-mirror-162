from setuptools import setup

setup(name='xyz_distro_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['xyz_distro_distributions'],
      zip_safe=False)
