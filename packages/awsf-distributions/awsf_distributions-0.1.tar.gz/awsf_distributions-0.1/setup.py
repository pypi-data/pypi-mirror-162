from setuptools import setup

setup(name='awsf_distributions',
      version='0.1',
      description='Binomial distributions and Gaussian distributions',
      packages=['awsf_distributions'],
      zip_safe=False)
