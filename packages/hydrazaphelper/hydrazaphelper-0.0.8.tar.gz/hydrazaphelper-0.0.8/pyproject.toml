# pyproject.toml

[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hydrazaphelper"
version = "0.0.8"
description = "A small wrapper for owasp zap"
authors = [{ name = "efe.pisirici", email = "efe.pisirici@hotmail.com" }]
license = { file = "LICENSE" }
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
keywords = []
dependencies = [
    "requests >= 2.26.0",
    "requests",
    'tomli; python_version > "3.0"',
]
requires-python = ">=3.0"

