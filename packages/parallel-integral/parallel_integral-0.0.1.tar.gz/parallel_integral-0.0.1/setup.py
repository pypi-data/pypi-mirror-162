import setuptools

setuptools.setup(
    name="parallel_integral",
    version="0.0.1",
    author="God, the one who wrote everything",
    description="A package to make integrals in parallel",
    packages=["parallel_code"]
)