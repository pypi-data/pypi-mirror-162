[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cgtcheck"
version = "0.0.1"
authors = [
  { name="Example Author" },
]
description = "cgtcheck package"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.7"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

