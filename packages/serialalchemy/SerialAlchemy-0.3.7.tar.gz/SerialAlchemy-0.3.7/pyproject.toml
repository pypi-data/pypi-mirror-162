[tool.poetry]
name = "SerialAlchemy"
version = "0.3.7"
description = "Mixin for serializing SQLAlchemy ORM classes into dict or json"
authors = ["Matt Schmidt <sloatx@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.7"
# sqlalchemy = {version = "^1.4.0", allow-prereleases = true}
sqlalchemy = {version = "^1.4.0"}

[tool.poetry.dev-dependencies]
pytest = "^6.0"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
