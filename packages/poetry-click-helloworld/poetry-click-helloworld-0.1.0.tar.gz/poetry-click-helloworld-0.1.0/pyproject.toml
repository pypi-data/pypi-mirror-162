[tool.poetry]
name = "poetry-click-helloworld"
version = "0.1.0"
description = ""
authors = ["Michael Ramos <michaeltramos@protonmail.com>"]

[tool.poetry.dependencies]
python = "^3.10"
click = "^8.1.3"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
pchw = "poetry_click_helloworld.main:main"
